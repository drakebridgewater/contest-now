services:
  backend:
    build: ./backend
    container_name: pdxmas-backend
    ports:
      - "3001:3001"
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    environment:
      - NODE_ENV=production
      - PORT=3001
      - DATABASE_PATH=/app/data/contest.db
      - BASE_URL=https://pdxmas.drakebridgewater.com
    restart: unless-stopped
    networks:
      - pdxmas-network

  frontend:
    build: ./frontend
    container_name: pdxmas-frontend
    ports:
      - "3099:80"
    environment:
      - REACT_APP_RESULTS_PASSWORD=pdxmas2025
#    depends_on:
#      backend:
#        condition: service_healthy
    restart: unless-stopped
    networks:
      - pdxmas-network

networks:
  pdxmas-network:
    driver: bridge
